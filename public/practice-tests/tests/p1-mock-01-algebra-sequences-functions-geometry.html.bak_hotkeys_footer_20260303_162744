
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>TMUA Mock Test 1 — Algebra & Geometry</title>
  <script type="text/javascript" async 
    src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"></script>
  <style>

.top-right {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 6px;

}
.options label {
  display: block;
}

#flag-btn {
  background-color: white;
  color: black;
  border: 1px solid black;
  font-size: 14px;
  font-weight: normal;
  padding: 6px 14px;
  border-radius: 4px;
  cursor: pointer;
}

#flag-btn.flagged {
  background-color: yellow;
  font-weight: bold;
  border: 2px solid #999;
}


    body {
      margin: 0;
      font-family: "Georgia", serif;
      background: white;
    }
    .header {
      background: #0074c1;
      color: white;
      padding: 10px 20px;
      font-size: 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .timer {
      font-size: 14px;
    }
    .container {
      padding: 20px 40px;
    }
    .question-number {
      font-weight: bold;
      font-size: 18px;
    }
    .options {
      margin-top: 20px;
      margin-bottom: 40px;
    }
    .option {
      margin: 10px 0;
    }
    input[type="radio"] {
      transform: scale(1.2);
      margin-right: 10px;
    }
    .footer {
      background: #0074c1;
      padding: 10px 20px;
      text-align: right;
    }
    .footer button {
      font-size: 16px;
      padding: 6px 16px;
      margin-left: 10px;
      background: #005fa3;
      color: white;
      border: none;
      border-radius: 3px;
      cursor: pointer;
    }
    #instructions {
      padding: 40px;
      font-size: 18px;
    }
    #exam-interface {
      display: none;
    }

/* âœ… START SCREEN ONLY â€” paste this at the VERY END of your <style> block.
   It only targets #instructions and wonâ€™t affect the exam UI. */

#instructions{
  max-width: 1250px;
  min-height: 560px;
  margin: 60px auto;
  background: #ffffff;
  border: 2px solid #f2b233;
  border-radius: 22px;
  box-shadow:
    0 18px 48px rgba(15, 23, 42, 0.12),
    0 10px 26px rgba(242, 178, 51, 0.18);
  padding: 64px 64px 70px;
  box-sizing: border-box;
  position: relative;
  z-index: 0;
}

/* grey page backdrop ONLY while #instructions is visible */
#instructions::before{
  content: "";
  position: fixed;
  inset: 0;
  background: #f3f5f7;
  z-index: -1;
}

/* title */
#instructions #test-title{
  margin: 0 0 18px 0;
  font-size: 30px;
  font-weight: 800;
  color: #0b1220;
}

/* paragraphs */
#instructions p{
  margin: 10px 0;
  color: #0f172a;
}

/* keep your existing flex row, just make spacing nicer */
#instructions > div:first-of-type{
  margin: 34px 0 26px !important;
}

/* inputs/selects â€” only visual polish (keeps your widths/padding from inline styles) */
#instructions input[type="text"],
#instructions input[type="email"],
#instructions select{
  border: 1.6px solid rgba(242, 178, 51, 0.65) !important;
  border-radius: 10px !important;
  background: #fff !important;
  outline: none !important;
}

#instructions input[type="text"]:focus,
#instructions input[type="email"]:focus,
#instructions select:focus{
  border-color: #f2b233 !important;
  box-shadow: 0 0 0 4px rgba(242, 178, 51, 0.18) !important;
}

/* start button â€” only visuals (doesnâ€™t change your inline font-size/padding if you keep them) */
#instructions button{
  background: #f6c44a !important;
  color: #2b1a00 !important;
  border: none !important;
  border-radius: 12px !important;
  box-shadow: 0 12px 24px rgba(246, 196, 74, 0.35) !important;
  cursor: pointer !important;
}

#instructions button:hover{
  filter: brightness(0.98);
  transform: translateY(-1px);
}

#instructions button:active{
  transform: translateY(0px);
  box-shadow: 0 10px 18px rgba(246, 196, 74, 0.28) !important;
}

/* optional: make the table feel â€œcard-likeâ€ but ONLY inside #instructions */
#instructions table{
  border-radius: 14px;
  overflow: hidden;
  background: #fff;
  box-shadow: 0 12px 24px rgba(15, 23, 42, 0.06);
}

/* responsive */
@media (max-width: 950px){
  #instructions{
    margin: 24px 16px;
    padding: 40px 22px 44px;
    min-height: auto;
  }
}
/* âœ… Only affects images inside questions (wonâ€™t touch your start screen) */
.question-media{
  margin: 14px 0 18px;
}

.question-media img{
  display: block;
  max-width: 320px;         /* âœ… smaller image */
  width: 100%;              /* responsive inside the max */
  height: auto;
  border: 1.5px solid #e5e7eb;   /* âœ… boxed */
  border-radius: 12px;
  padding: 10px;            /* âœ… space inside the box */
  background: #ffffff;

}

/* Optional: center the boxed image */
.question-media{
  display: flex;
  justify-content: center;
}
/* âœ… Bigger text for the QUESTION AREA only (safe: doesnâ€™t touch header/footer/start screen) */
.container{
  font-size: 19px;           /* base size for question text */
  line-height: 1.6;
}

/* Question number bigger */
.container .question-number{
  font-size: 22px;
  font-weight: 800;
  margin-bottom: 10px;
}

/* Paragraphs spacing + size */
.container p{
  font-size: 19px;
  line-height: 1.65;
  margin: 12px 0;
}

/* Options bigger */
.container .options{
  margin-top: 18px;
  margin-bottom: 40px;
}

.container .options .option{
  font-size: 19px;
  line-height: 1.8;
}

/* Radio slightly larger */
.container input[type="radio"]{
  transform: scale(1.35);
  margin-right: 12px;
}

  </style>

</head>
<body>

<div id="instructions">
    <h2 id="test-title">TMUA MOCK TEST 1</h2>


  <p><strong>Time allowed:</strong> 1 hour and 15 minutes</p>
  <p><strong>Calculators are not permitted.</strong></p>


<div style="display: flex; align-items: flex-end; gap: 20px; flex-wrap: wrap; margin: 20px 0;">
  <div>
    <label for="student-name" style="font-size: 16px;">Enter your full name:</label><br>
    <input type="text" id="student-name" placeholder="Full Name" style="padding: 8px; font-size: 16px; width: 240px; margin-top: 5px;">
  </div>

  <div>
    <label for="studentEmail" style="font-size: 16px;">Email (to receive results):</label><br>
    <input type="email" id="studentEmail" placeholder="example@email.com" style="padding: 8px; font-size: 16px; width: 240px; margin-top: 5px;">
  </div>

  <div>
    <label for="test-date" style="font-size: 16px;">Select your AMC test session:</label><br>
    <select id="test-date" style="padding: 8px; font-size: 16px; width: 240px; margin-top: 5px;">
      <option value="October 2025">October 2026</option>
      <option value="January 2026">January 2027</option>
      <option value="October 2026">October 2027</option>
    </select>
  </div>

  <div style="margin-top: 28px;">
    <button onclick="startExam()" style="font-size: 16px; padding: 10px 20px;">ðŸŽ¯ Start Exam</button>
  </div>
</div>


<table style="border-collapse: collapse; width: 100%; font-size: 16px; margin-top: 20px;">
  <thead>
    <tr style="background: #f2f2f2;">
      <th style="border: 1px solid #ccc; padding: 10px;">Section</th>
      <th style="border: 1px solid #ccc; padding: 10px;">Topics Covered</th>
      <th style="border: 1px solid #ccc; padding: 10px;">No. of Questions</th>
      <th style="border: 1px solid #ccc; padding: 10px;">Time</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="border: 1px solid #ccc; padding: 10px; text-align: center;">Paper 1</td>
      <td style="border: 1px solid #ccc; padding: 10px;">
        <ul style="margin: 0; padding-left: 18px;">
          <li>Algebra</li>
          <li>Functions</li>
          <li>Sequences</li>
          <li>Binomial</li>
        </ul>
      </td>
      <td style="border: 1px solid #ccc; padding: 10px; text-align: center;">20</td>
      <td style="border: 1px solid #ccc; padding: 10px; text-align: center;">75 minutes</td>
    </tr>
  </tbody>
</table>

<p style="font-size: 16px; margin-top: 20px;">
 
  Choose the one correct answer for each question. There are <strong>no penalties for incorrect answers</strong> â€” marks are awarded only for correct responses.
  You should attempt <strong>all 20 questions</strong>.
</p>

<p style="font-size: 16px;">
  A countdown timer will begin once the test starts. You may navigate between questions at any time. The test will automatically submit when the time expires.
</p>


  <p style="font-weight: bold; font-size: 16px;">When you're ready, click the START EXAM button at the top to begin.</p>

</div>


<div id="exam-interface">
  <div class="header">
    <div><strong>(TMUA) Test of Mathematics for University Admission</strong></div>
    
    <div class="top-right">
      <div class="timer">
        â²ï¸ Time Remaining <span id="time">75:00</span><br>
        ðŸ“„ <span id="counter">1 of 20 (Paper 1)</span>
      </div>
      <button id="flag-btn" onclick="toggleFlag()">   âš‘ Flag for Review   </button>
    </div>
  </div>

  <div class="container" id="question-container">
    <!-- Questions will appear here -->
  </div>

  <div class="footer">
    <button id="prev-btn" onclick="prevQuestion()">&larr; Previous</button>
    <button id="next-btn" onclick="nextQuestion()">Next &rarr;</button>
    <button onclick="openNavigator()">Navigator</button>
    <button onclick="confirmSubmit()">Submit</button>
  </div>
</div>


<script>
const solutionPDF = "https://www.thrivingscholars.com/_files/ugd/98f2c5_bf9ea7f03095418981f232b72659cbe3.pdf";

let flags = new Array(20).fill(false); // Assuming 20 questions per section
let timeSpent = new Array(20).fill(0); // 20 questions
let lastTimestamp = Date.now(); // timestamp when current question was shown

let studentName = "";
let testSession = "";

function nextQuestion() {
  if (current < questions.length - 1) {
    timeSpent[current] += (Date.now() - lastTimestamp) / 1000; // record time spent
    current++;
    lastTimestamp = Date.now(); // reset timestamp for new question
    renderQuestion();
  }
}


function openNavigator() {
  document.getElementById("navigator-popup").style.display = "block";
}

function closeNavigator() {
  document.getElementById("navigator-popup").style.display = "none";
}

function jumpTo(index) {
timeSpent[current] += (Date.now() - lastTimestamp) / 1000;
current = index;
lastTimestamp = Date.now();
renderQuestion();
closeNavigator();
updateFlagButton();


}


function renderQuestion() {
  container.innerHTML = questions[current];
  counter.textContent = `${current + 1} of ${questions.length}`;
  MathJax.typesetPromise();
updateFlagButton();

  const inputs = document.querySelectorAll(`input[name='q${current + 1}']`);
  inputs.forEach(input => {
    if (userAnswers[current] === input.value) {
      input.checked = true;
    }
    input.addEventListener('change', () => {
      userAnswers[current] = input.value;
    });
  });

  // Disable Next button on last question
  const nextBtn = document.getElementById("next-btn");
  if (nextBtn) nextBtn.disabled = (current === questions.length - 1);

  // Disable Previous button on first question
  const prevBtn = document.getElementById("prev-btn");
  if (prevBtn) prevBtn.disabled = (current === 0);
}

function toggleFlag() {
  flags[current] = !flags[current];
  updateFlagButton();
  updateNavigatorButton(current);
}

function updateFlagButton() {
  const flagBtn = document.getElementById("flag-btn");
  if (!flagBtn) return;
  if (flags[current]) {
    flagBtn.classList.add("flagged");
  } else {
    flagBtn.classList.remove("flagged");
  }
}

  function confirmSubmit() {
  const confirmBox = confirm("Are you sure you want to submit the test?");
  if (confirmBox) {
    submitExam();
  }
}
  function confirmSubmit() {
  const confirmBox = confirm("Are you sure you want to submit the test?");
  if (confirmBox) {
    submitExam();
  }
}
  const correctAnswers = [
    "B", "D", "B", "C", "D",
    "D", "D", "B", "D", "C",
    "D", "B", "B", "D", "C",
    "A", "E", "C", "C", "D"
  ];

  let userAnswers = new Array(20).fill(null);
  let current = 0;
  let minutes = 75;
  let seconds = 0;
  let timerInterval;

  const timerDisplay = document.getElementById("time");
  const container = document.getElementById("question-container");
  const counter = document.getElementById("counter");

  const questions = [

    `<div class="question-number">Question 1</div><p>The expansion of \\( (a - bx)^c \\) is</p>
    <p>\\[
4 - px + 108x^2 - qx^3 + rx^4
\\]</p>
    <p>where \\( a, b, c, p, q, r \\) are positive real constants.</p>
    <p>Find the value of \\( p + q + r \\).</p>
      <div class="options">
        <label class="option"><input type="radio" name="q1" value="A"> \\( 81 - 84\\sqrt{2} \\)</label><br>
        <label class="option"><input type="radio" name="q1" value="B"> \\( 81 + 132\\sqrt{2} \\)</label><br>
        <label class="option"><input type="radio" name="q1" value="C">  \\( 132\\sqrt{2} - 81 \\)</label><br>
        <label class="option"><input type="radio" name="q1" value="D"> \\( 81 + 84\\sqrt{2} \\)</label>
      </div>`,

    `<div class="question-number">Question 2</div><p>
  The coefficient of \\( x^{11} \\) in the expansion of \\( (2 + x^2 + x^3)^8 \\) is equal to 28 times the coefficient of \\( x^2 \\) in \\( (2 + ax)^6 \\). 
  Find all the possible values of the constant \\( a \\).
</p>
<div class="options">
  <label class="option"><input type="radio" name="q2" value="A"> \\( \\pm 2\\sqrt{7} \\)</label><br>
  <label class="option"><input type="radio" name="q2" value="B"> \\( \\pm \\frac{1}{2} \\)</label><br>
  <label class="option"><input type="radio" name="q2" value="C"> \\( \\pm \\frac{1}{4} \\)</label><br>
  <label class="option"><input type="radio" name="q2" value="D"> \\( \\pm 1 \\)</label>
</div>`,

    `<div class="question-number">Question 3</div><p>The coefficient of \\( x^2 \\) in the expansion of \\( (2 + bx)^4 \\) is 2 times the coefficient of \\( x^3 \\) in \\( (1 + bx)^6 \\). Given that \\( b \\ne 0 \\), what is the value of \\( b \\)?</p>
      <div class="options">
        <label class="option"><input type="radio" name="q3" value="A"> \\( \\frac{8}{15} \\)</label><br>
        <label class="option"><input type="radio" name="q3" value="B">\\( \\frac{3}{5} \\)</label><br>
        <label class="option"><input type="radio" name="q3" value="C">\\( \\frac{3}{20} \\)</label><br>
        <label class="option"><input type="radio" name="q3" value="D">\\( \\frac{6}{5} \\)</label>
      </div>`,

    `<div class="question-number">Question 4</div><p> Find the coefficient of x</p> <p> \\[ (1 - x)^0 - (1 - x)^1 + (1 - x)^2 - (1 - x)^3 + (1 - x)^4 - \\cdots + (1 - x)^{50} \\] </p>
      <div class="options">
        <label class="option"><input type="radio" name="q4" value="A"> 51</label><br>
        <label class="option"><input type="radio" name="q4" value="B"> -51</label><br>
        <label class="option"><input type="radio" name="q4" value="C"> -25</label><br>
        <label class="option"><input type="radio" name="q4" value="D"> 25</label>
      </div>`,

    `<div class="question-number">Question 5</div><p>Find the value of the expression</p> <p> \\[ \\sqrt{16 + 8\\sqrt{3} + 3} - \\sqrt{12 - 4\\sqrt{3} + 1} \\] </p>
      <div class="options">
        <label class="option"><input type="radio" name="q5" value="A"> \\( \\sqrt{6 + 12\\sqrt{3}} \\)</label><br>
        <label class="option"><input type="radio" name="q5" value="B"> \\( \\sqrt{3} - 5 \\)</label><br>
        <label class="option"><input type="radio" name="q5" value="C"> 3</label><br>
        <label class="option"><input type="radio" name="q5" value="D"> \\( 5 - \\sqrt{3} \\)</label>
      </div>`,

    `<div class="question-number">Question 6</div><p> The circles with equations \\( (x - r)^2 + (y - 2)^2 = r^2 \\) and \\( (x + r)^2 + (y + r)^2 = 4r^2 \\) touch precisely once when </p>
      <div class="options">
        <label class="option"><input type="radio" name="q6" value="A"> \\( r = \\sqrt{3} - 1 \\)</label><br>
        <label class="option"><input type="radio" name="q6" value="B"> \\( r = \\frac{8}{5} \\)</label><br>
        <label class="option"><input type="radio" name="q6" value="C"> \\( r = \\frac{2}{1 + \\sqrt{5}} \\)</label><br>
        <label class="option"><input type="radio" name="q6" value="D"> \\( r = \\frac{1 + \\sqrt{5}}{2} \\)</label>
      </div>`,

    `<div class="question-number">Question 7</div><p> Find the value(s) of \\( a \\) such that the turning point of the parabola \\( y = x^2 + 2ax + 1 \\) is closest to the origin. </p>
      <div class="options">
        <label class="option"><input type="radio" name="q7" value="A"> \\( a = \\sqrt{2} \\)</label><br>
        <label class="option"><input type="radio" name="q7" value="B"> \\( a = \\pm \\sqrt{2} \\)</label><br>
        <label class="option"><input type="radio" name="q7" value="C"> \\( a = \\frac{\\sqrt{2}}{2} \\)</label><br>
        <label class="option"><input type="radio" name="q7" value="D"> \\( a = \\pm \\frac{1}{\\sqrt{2}} \\)</label>
      </div>`,

    `<div class="question-number">Question 8</div><p> The line \\( y = 2x + c \\) is such that it intersects the circle \\( x^2 + y^2 = 9 \\) at two points \\( A \\) and \\( B \\). \\( M \\) is the midpoint of the chord \\( AB \\) of the circle. The equation of the locus of \\( M \\) as \\( c \\) varies between \\( -3\\sqrt{5} \\) and \\( 3\\sqrt{5} \\) is </p>
      <div class="options">
        <label class="option"><input type="radio" name="q8" value="A"> \\( x^2 + y^2 = 2 \\)</label><br>
        <label class="option"><input type="radio" name="q8" value="B"> \\( y = -\\frac{1}{2}x \\)</label><br>
        <label class="option"><input type="radio" name="q8" value="C"> \\( y = \\frac{1}{2}x \\)</label><br>
        <label class="option"><input type="radio" name="q8" value="D"> \\( y = x^2 \\)</label>
      </div>`,

    `<div class="question-number">Question 9</div><p> The tangent to the circle \\( x^2 + y^2 + 6x + 2y + 2 = 0 \\) at the point \\( (-5, 1) \\) passes through the point \\( (-3, 3) \\). The other tangent to the circle that passes through this point touches the circle at the point </p>
      <div class="options">
        <label class="option"><input type="radio" name="q9" value="A"> \\( (-3, 2\\sqrt{2}) \\)</label><br>
        <label class="option"><input type="radio" name="q9" value="B"> \\( (-3, 1) \\)</label><br>
        <label class="option"><input type="radio" name="q9" value="C"> \\( (-1, 2\\sqrt{2}) \\)</label><br>
        <label class="option"><input type="radio" name="q9" value="D"> \\( (-1, 1) \\)</label>
      </div>`,

    `<div class="question-number">Question 10</div><p> The circle \\( x^2 + y^2 + 2ax + 2by = c \\) encloses the point \\( (1, 1) \\) precisely when </p>
      <div class="options">
        <label class="option"><input type="radio" name="q10" value="A"> \\( c > 2(1 - a - b) \\)</label><br>
        <label class="option"><input type="radio" name="q10" value="B"> \\( a^2 + b^2 < 1 \\)</label><br>
        <label class="option"><input type="radio" name="q10" value="C"> \\( c > 2(a + b + 1) \\)</label><br>
        <label class="option"><input type="radio" name="q10" value="D"> \\( a^2 + 2ab + b^2 < c \\)</label>
      </div>`,

    `<div class="question-number">Question 11</div><p> Which of the following is a tangent to the circle \\( (x - 2)^2 + (y - 3)^2 = 4 \\)? </p>
      <div class="options">
        <label class="option"><input type="radio" name="q11" value="A"> \\( x = 3 + \\sqrt{2} \\)</label><br>
        <label class="option"><input type="radio" name="q11" value="B"> \\( y = 5 + \\frac{\\sqrt{2}}{2} - x \\)</label><br>
        <label class="option"><input type="radio" name="q11" value="C"> \\( y = 2(2 - \\sqrt{2}) \\)</label><br>
        <label class="option"><input type="radio" name="q11" value="D"> \\( y = x + 1 - 2\\sqrt{2} \\)</label>
      </div>`,

    `<div class="question-number">Question 12</div><p> The reflection of the point \\( (a, a) \\) in the line \\( y = \\frac{1}{a}x \\) where \\( a \\ne 0 \\) is </p>
      <div class="options">
        <label class="option"><input type="radio" name="q12" value="A"> \\( \\left( \\frac{a(a - 1)^2}{a^2 + 1}, \\frac{a^2(a^2 + 3)}{a^2 + 1} \\right) \\)</label><br>
        <label class="option"><input type="radio" name="q12" value="B"> \\( \\left( \\frac{a(a^2 + 2a - 1)}{a^2 + 1}, \\frac{-a(a^2 - 2a - 1)}{a^2 + 1} \\right) \\)</label><br>
        <label class="option"><input type="radio" name="q12" value="C"> \\( \\left( \\frac{a(a - 1)^2}{a^2 + 1}, \\frac{-a(a - 1)^2}{a^2 + 1} \\right) \\)</label><br>
        <label class="option"><input type="radio" name="q12" value="D"> \\( \\left( \\frac{a(a + 1)^2}{a^2 + 1}, \\frac{a^2(a + 1)^2}{a^2 + 1} \\right) \\)</label>
      </div>`,

    `<div class="question-number">Question 13</div><p> The circle \\( C \\) has equation \\( x^2 + y^2 = 1 \\) and is intersected at \\( (0,1) \\) and \\( (0,-1) \\) by two circles of radius \\( r \\), one with a centre at \\( (a, 0) \\) and the other with a centre at \\( (-a, 0) \\). This is shown in the diagram. </p> <p> <img src="https://i.ibb.co/dswXQ4HJ/Picture6.png" alt="Picture6" style="width: 400px; height: 250px;">
 </p> <p> The value of \\( a \\) that results in \\( C \\) having three distinct regions of equal area satisfies the equation: </p>
      <div class="options">
        <label class="option"><input type="radio" name="q13" value="A"> \\( \\frac{\\pi(a^2 + 1) \\arctan\\left(\\frac{1}{a}\\right)}{90} - 6a = \\pi \\)</label><br>
        <label class="option"><input type="radio" name="q13" value="B">   \\( \\frac{\\pi(a^2 + 1) \\arctan\\left(\\frac{1}{a}\\right)}{30} - 6a = \\pi \\)</label><br>
        <label class="option"><input type="radio" name="q13" value="C"> \\( \\pi(a^2 + 1) \\arctan\\left(\\frac{1}{a}\\right) - 6a = \\pi \\)</label><br>
        <label class="option"><input type="radio" name="q13" value="D">\\( \\frac{(a^2 + 1) \\arctan\\left(\\frac{1}{a}\\right)}{30} = 1 \\)</label>
      </div>`,

    `<div class="question-number">Question 14</div><p>The function \\( f \\) is defined on the positive integers as follows:</p> <p>\\( f(1) = 2 \\)</p> <p>For \\( n \\geq 1 \\):</p> <p>\\( f(n+1) = 5f(n) + 1 \\), if \\( f(n) \\) is odd</p> <p>\\( f(n+1) = \\frac{1}{2} f(n) \\), if \\( f(n) \\) is even</p> <p>What is the value of \\( \\sum\\limits_{r=1}^{100} f(r) \\)?</p>
      <div class="options">
        <label class="option"><input type="radio" name="q14" value="A"> 535</label><br>
        <label class="option"><input type="radio" name="q14" value="B">546</label><br>
        <label class="option"><input type="radio" name="q14" value="C">  560</label><br>
        <label class="option"><input type="radio" name="q14" value="D">563</label>
      </div>`,

    `<div class="question-number">Question 15</div><p> When \\( 2x^2 + 3x - 15 \\) is multiplied by \\( (ax - 4) \\) and the resulting product is divided by \\( x + 2 \\), the remainder is 182. </p>
      <div class="options">
        <label class="option"><input type="radio" name="q15" value="A"> \\( -9 \\)</label><br>
        <label class="option"><input type="radio" name="q15" value="B">  \\( -5 \\)</label><br>
        <label class="option"><input type="radio" name="q15" value="C"> \\( 5 \\)</label><br>
        <label class="option"><input type="radio" name="q15" value="D"> \\( 9 \\)</label>
      </div>`,

    `<div class="question-number">Question 16</div><p> Find the non-zero solution to the equation: </p> <p> \\[ \\frac{3^{(16^x)}}{81^{(4^x)}} = \\frac{1}{27} \\] </p>
      <div class="options">
        <label class="option"><input type="radio" name="q16" value="A"> \\( \\log_4 3 \\)</label><br>
        <label class="option"><input type="radio" name="q16" value="B"> \\( \\log_16 3 \\)</label><br>
        <label class="option"><input type="radio" name="q16" value="C"> \\( 1 \\)</label><br>
        <label class="option"><input type="radio" name="q16" value="D"> \\( 2 \\)</label>
      </div>`,


    `<div class="question-number">Question 17</div><p> Let \\( n \\) be a positive integer. </p> <p> \\( x^2 + 2 \\) is a factor of </p> <p> \\[ (x^4 - 5)^n - (x^2 + 1)^{n+1} + (x^2 + 4)^n (3 + x^2)^n \\] </p> <p> for: </p>
      <div class="options">
        <label class="option"><input type="radio" name="q17" value="A"> odd \\( n \\)</label><br>
        <label class="option"><input type="radio" name="q17" value="B">even \\( n \\)</label><br>
        <label class="option"><input type="radio" name="q17" value="C">  no \\( n \\)</label><br>
        <label class="option"><input type="radio" name="q17" value="D"> all \\( n \\)</label><br>
       <label class="option"><input type="radio" name="q17" value="E"> \\( n = 1 \\)</label>

      </div>`,

    `<div class="question-number">Question 18</div><p> \\( a \\) and \\( b \\) are non-zero integers. </p> <p> When the polynomial \\( x^2 - 2ax - a^2 \\) is divided by \\( x - b \\), the remainder is 1. </p> <p> The polynomial \\( 4bx^2 - 6x + 6 \\) has \\( 2x - a \\) as a factor. </p> <p> It follows that \\( a + b \\) equals: </p>
      <div class="options">
        <label class="option"><input type="radio" name="q18" value="A">\\( -5 \\)</label><br>
        <label class="option"><input type="radio" name="q18" value="B">\\( -3 \\)</label><br>
        <label class="option"><input type="radio" name="q18" value="C">  \\( -1 \\)</label><br>
        <label class="option"><input type="radio" name="q18" value="D"> \\( 0 \\)</label>
      </div>`,


    `<div class="question-number">Question 19</div><p> The polynomial \\( p_n \\) is defined by: </p> <p> \\[ p_n(x) = (x - 2n + 1) + (2x - 2n + 3) + (3x - 2n + 5) + \\cdots + (nx - 1) \\] </p> <p> Given that \\( n \\geq 2 \\), what is the remainder when \\( p_n(x) \\) is divided by \\( p_{n-1}(x) \\)? </p>
      <div class="options">
        <label class="option"><input type="radio" name="q19" value="A"> \\( \\frac{n^2 - 1}{2} \\)</label><br>
        <label class="option"><input type="radio" name="q19" value="B">  \\( n \\)</label><br>
        <label class="option"><input type="radio" name="q19" value="C">  \\( -1 \\)</label><br>
        <label class="option"><input type="radio" name="q19" value="D">  \\( 1 \\)</label>
      </div>`,

    `<div class="question-number">Question 20</div><p> Given that \\( x, y \\) and \\( z \\) are positive real numbers, the equations </p> <p> \\[ 4\\log_z x = y, \\quad \\log_x z = y, \\quad x + \\log_y z = 0 \\] </p> <p>...</p>
      <div class="options">
        <label class="option"><input type="radio" name="q20" value="A"> have a unique solution for \\( x \\) but not for \\( y \\) and \\( z \\).</label><br>
        <label class="option"><input type="radio" name="q20" value="B"> have unique solutions for \\( x \\) and \\( y \\) but infinitely many solutions for \\( z \\).</label><br>
        <label class="option"><input type="radio" name="q20" value="C"> have unique solutions for \\( x \\) and \\( z \\) but infinitely many solutions for \\( y \\).</label><br>
        <label class="option"><input type="radio" name="q20" value="D"> have a unique solution for \\( x, y \\) and \\( z \\).</label>
      </div>`,

  ];



function startExam() {
  const nameInput = document.getElementById("student-name").value.trim();
  const emailInput = document.getElementById("studentEmail").value.trim();
  const testSelect = document.getElementById("test-date").value;

  if (!nameInput) {
    alert("Please enter your full name before starting the exam.");
    return;
  }

  if (!emailInput || !emailInput.includes("@")) {
    alert("Please enter a valid email address.");
    return;
  }

  studentName = nameInput;
  testSession = testSelect;

  document.getElementById("instructions").style.display = "none";
  document.getElementById("exam-interface").style.display = "block";
  timerInterval = setInterval(updateTimer, 1000);
  renderQuestion();
}



  function updateTimer() {
    if (minutes === 0 && seconds === 0) {
      clearInterval(timerInterval);
      submitExam();
      return;
    }
    if (seconds === 0) {
      minutes--;
      seconds = 59;
    } else {
      seconds--;
    }
    const mm = minutes.toString().padStart(2, '0');
    const ss = seconds.toString().padStart(2, '0');
    timerDisplay.textContent = `${mm}:${ss}`;
  }

  
function prevQuestion() {
  if (current > 0) {
    timeSpent[current] += (Date.now() - lastTimestamp) / 1000; // record time spent
    current--;
    lastTimestamp = Date.now(); // reset timestamp for new question
    renderQuestion();
  }
}

function updateNavigatorButton(index) {
  const btn = document.getElementById(`nav-btn-${index}`);
  if (!btn) return;
  if (flags[index]) {
    btn.style.backgroundColor = "gold";
    btn.style.fontWeight = "bold";
    btn.textContent = `âš‘ ${index + 1}`;
  } else {
    btn.style.backgroundColor = "";
    btn.style.fontWeight = "normal";
    btn.textContent = `${index + 1}`;
  }
}

function submitExam() {
  clearInterval(timerInterval);

  // â±ï¸ Record final question time
  timeSpent[current] += (Date.now() - lastTimestamp) / 1000;

  // âœ… Calculate score
  let score = 0;
  for (let i = 0; i < correctAnswers.length; i++) {
    if (userAnswers[i] === correctAnswers[i]) {
      score++;
    }
  }

  // âœ… Prepare timestamp
  const timestamp = new Date().toLocaleString();

  // âœ… Find incorrect question numbers
  let incorrectList = [];
  for (let i = 0; i < correctAnswers.length; i++) {
    if (userAnswers[i] !== correctAnswers[i]) {
      incorrectList.push(i + 1);
    }
  }

let timeTableHTML = "";
for (let i = 0; i < 20; i++) {
  const seconds = Math.round(timeSpent[i]);
  const mm = String(Math.floor(seconds / 60)).padStart(2, '0');
  const ss = String(seconds % 60).padStart(2, '0');
  const yourAns = userAnswers[i] ?? "-";
  const correctAns = correctAnswers[i];
  const status = yourAns === correctAns ? "âœ…" : (yourAns === "-" ? "â€”" : "âŒ");

  timeTableHTML += `
    <tr>
      <td style="border: 1px solid #ccc; padding: 6px;">${i + 1}</td>
      <td style="border: 1px solid #ccc; padding: 6px;">${yourAns}</td>
      <td style="border: 1px solid #ccc; padding: 6px;">${correctAns}</td>
      <td style="border: 1px solid #ccc; padding: 6px;">${mm}:${ss}</td>
      <td style="border: 1px solid #ccc; padding: 6px;">${status}</td>
    </tr>\n`; // âœ… Keep line break
}

let plainTextSummary = "Paper 1 Analysis\n";

for (let i = 0; i < 20; i++) {
  const seconds = Math.round(timeSpent[i]);
  const mm = String(Math.floor(seconds / 60)).padStart(2, '0');
  const ss = String(seconds % 60).padStart(2, '0');
  const yourAns = userAnswers[i] ?? "-";
  const correctAns = correctAnswers[i];
  plainTextSummary += `Q.${i + 1}  Correct: ${correctAns}   Yours: ${yourAns}   Time: ${mm}:${ss}\n`;
}


const studentEmail = document.getElementById("studentEmail").value;
const testTitle = document.getElementById("test-title").textContent.trim();

emailjs.send("service_5l4w8x2", "template_j1jrs4o", {
  name: studentName,
  session: testSession,
  score: `${score} / ${correctAnswers.length}`,
  timestamp: new Date().toLocaleString(),
  incorrect: incorrectList.length > 0 ? incorrectList.join(", ") : "None",
  paper1: plainTextSummary.trim(),
  to_email: studentEmail,
  solution_link: solutionPDF,
  test_title: testTitle
})
.then(res => console.log("âœ… Email sent", res))
.catch(err => console.error("âŒ Email error", err));



  // âœ… Select emoji
  let emoji = score >= 14 ? "ðŸ˜Š"  : "ðŸ˜";

  // âœ… Generate time table
  let timeRows = "";
  for (let i = 0; i < timeSpent.length; i++) {
    const seconds = Math.round(timeSpent[i]);
    const mm = String(Math.floor(seconds / 60)).padStart(2, '0');
    const ss = String(seconds % 60).padStart(2, '0');
    const status = userAnswers[i] ? (userAnswers[i] === correctAnswers[i] ? "âœ…" : "âŒ") : "â€”";
    timeRows += `
      <tr>
        <td style="border: 1px solid #ccc; padding: 8px;">${i + 1}</td>
        <td style="border: 1px solid #ccc; padding: 8px;">${userAnswers[i] || "-"}</td>
        <td style="border: 1px solid #ccc; padding: 8px;">${correctAnswers[i]}</td>
        <td style="border: 1px solid #ccc; padding: 8px;">${mm}:${ss}</td>
        <td style="border: 1px solid #ccc; padding: 8px;">${status}</td>
      </tr>`;
  }

  // âœ… Display final screen
  container.innerHTML = `
    <div style="display: flex; flex-direction: column; align-items: center; padding: 40px;">
      <div style="font-size: 160px;">${emoji}</div>
      <h1>Exam Completed</h1>
<h1>The detailed Score Report has been emailed to you âœ… </h1>

      <table style="border-collapse: collapse; margin-top: 30px; font-size: 18px;">
        <tr style="background: #f9f9f9;">
          <td style="border: 1px solid #ccc; padding: 10px 20px;"><strong>Name</strong></td>
          <td style="border: 1px solid #ccc; padding: 10px 20px;">${studentName}</td>
        </tr>
        <tr style="background: #f9f9f9;">
          <td style="border: 1px solid #ccc; padding: 10px 20px;"><strong>Your Score</strong></td>
          <td style="border: 1px solid #ccc; padding: 10px 20px;"><strong>${score} / 20</strong></td>
        </tr>
        <tr>
          <td style="border: 1px solid #ccc; padding: 10px 20px;">Threshold Score</td>
          <td style="border: 1px solid #ccc; padding: 10px 20px;">15+ / 20</td>
        </tr>
        <tr>
          <td style="border: 1px solid #ccc; padding: 10px 20px;">Average Test Taker Score</td>
          <td style="border: 1px solid #ccc; padding: 10px 20px;">8 / 20</td>
        </tr>
      </table>

      <h3 style="margin-top: 40px;">Paper 1 Analysis</h3>
      <table style="border-collapse: collapse; margin-top: 10px; font-size: 16px; width: 100%; max-width: 600px;">
        <thead style="background: #eee;">
          <tr>
            <th style="border: 1px solid #ccc; padding: 8px;">Q#</th>
            <th style="border: 1px solid #ccc; padding: 8px;">Your Ans</th>
            <th style="border: 1px solid #ccc; padding: 8px;">Correct</th>
            <th style="border: 1px solid #ccc; padding: 8px;">Time</th>
            <th style="border: 1px solid #ccc; padding: 8px;">Status</th>
          </tr>
        </thead>
        <tbody>${timeRows}</tbody>
      </table>

      <h3 style="margin-top: 40px;">ðŸ“„ View Full Solutions</h3>
  <iframe 
  id="solution-frame"
  width="100%" 
  height="600px" 
  style="border: 1px solid #ccc; margin-top: 10px;">
</iframe>
    </div>
  `;
document.getElementById("solution-frame").src = solutionPDF;

  document.querySelector(".footer").style.display = "none";
}

function generateNavigatorButtons() {
  const grid = document.getElementById("navigator-grid");
  for (let i = 0; i < 20; i++) {
    const btn = document.createElement("button");
    btn.id = `nav-btn-${i}`;
    btn.textContent = i + 1;
    btn.style.padding = "10px";
    btn.onclick = function () {
      jumpTo(i);
    };
    grid.appendChild(btn);
  }
}

window.onload = function () {
  generateNavigatorButtons();
  for (let i = 0; i < flags.length; i++) {
    updateNavigatorButton(i);
  }
};


</script>
<div id="navigator-popup" style="display:none; position:fixed; top:20%; left:50%; transform:translateX(-50%);
 background:white; border:2px solid #0074c1; border-radius:10px; padding:20px; z-index:9999; width:300px; box-shadow:0 5px 15px rgba(0,0,0,0.3);">
  <h3 style="margin-top: 0; color:#0074c1;">Jump to Question</h3>
  <div id="navigator-grid" style="display:grid; grid-template-columns: repeat(5, 1fr); gap:10px; margin-bottom: 20px;">
    <!-- Buttons will be generated here -->
  </div>
  <button onclick="closeNavigator()" style="background:#ccc; padding:6px 12px;">Close</button>
</div>
<script src="https://cdn.jsdelivr.net/npm/emailjs-com@3/dist/email.min.js"></script>
<script>
  (function () {
    emailjs.init("9jjn3yBHl6S88hITy"); // Replace with your actual public key
  })();
</script>

</body>
</html>

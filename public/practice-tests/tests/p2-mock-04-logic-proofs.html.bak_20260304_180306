
<!-- 
1. <h2 id="test-title">TMUA MOCK TEST 2</h2>
2. const correctAnswers
3. const questions = [
4. const solutionPDF =
5. let plainTextSummary =
6. <ul style="margin: 0; padding-left: 18px;">
7. <td style="border:
-->


<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>TMUA Mock Test 4 - Paper 2 (Logic and Proofs)</title>
  <script type="text/javascript" async 
    src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"></script>
  <style>
.top-right {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 6px;

}
.options label {
  display: block;
}

#flag-btn {
  background-color: white;
  color: black;
  border: 1px solid black;
  font-size: 14px;
  font-weight: normal;
  padding: 6px 14px;
  border-radius: 4px;
  cursor: pointer;
}

#flag-btn.flagged {
  background-color: yellow;
  font-weight: bold;
  border: 2px solid #999;
}


    body {
      margin: 0;
      font-family: "Georgia", serif;
      background: white;
    }
    .header {
      background: #0074c1;
      color: white;
      padding: 10px 20px;
      font-size: 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .timer {
      font-size: 14px;
    }
    .container {
      padding: 20px 40px;
    }
    .question-number {
      font-weight: bold;
      font-size: 18px;
    }
    .options {
      margin-top: 20px;
      margin-bottom: 40px;
    }
    .option {
      margin: 10px 0;
    }
    input[type="radio"] {
      transform: scale(1.2);
      margin-right: 10px;
    }
    .footer {
      background: #0074c1;
      padding: 10px 20px;
      text-align: right;
    }
    .footer button {
      font-size: 16px;
      padding: 6px 16px;
      margin-left: 10px;
      background: #005fa3;
      color: white;
      border: none;
      border-radius: 3px;
      cursor: pointer;
    }
    #instructions {
      padding: 40px;
      font-size: 18px;
    }
    #exam-interface {
      display: none;
    }

/* ✅ START SCREEN ONLY — paste this at the VERY END of your <style> block.
   It only targets #instructions and won’t affect the exam UI. */

#instructions{
  max-width: 1250px;
  min-height: 560px;
  margin: 60px auto;
  background: #ffffff;
  border: 2px solid #f2b233;
  border-radius: 22px;
  box-shadow:
    0 18px 48px rgba(15, 23, 42, 0.12),
    0 10px 26px rgba(242, 178, 51, 0.18);
  padding: 64px 64px 70px;
  box-sizing: border-box;
  position: relative;
  z-index: 0;
}

/* grey page backdrop ONLY while #instructions is visible */
#instructions::before{
  content: "";
  position: fixed;
  inset: 0;
  background: #f3f5f7;
  z-index: -1;
}

/* title */
#instructions #test-title{
  margin: 0 0 18px 0;
  font-size: 30px;
  font-weight: 800;
  color: #0b1220;
}

/* paragraphs */
#instructions p{
  margin: 10px 0;
  color: #0f172a;
}

/* keep your existing flex row, just make spacing nicer */
#instructions > div:first-of-type{
  margin: 34px 0 26px !important;
}

/* inputs/selects — only visual polish (keeps your widths/padding from inline styles) */
#instructions input[type="text"],
#instructions input[type="email"],
#instructions select{
  border: 1.6px solid rgba(242, 178, 51, 0.65) !important;
  border-radius: 10px !important;
  background: #fff !important;
  outline: none !important;
}

#instructions input[type="text"]:focus,
#instructions input[type="email"]:focus,
#instructions select:focus{
  border-color: #f2b233 !important;
  box-shadow: 0 0 0 4px rgba(242, 178, 51, 0.18) !important;
}

/* start button — only visuals (doesn’t change your inline font-size/padding if you keep them) */
#instructions button{
  background: #f6c44a !important;
  color: #2b1a00 !important;
  border: none !important;
  border-radius: 12px !important;
  box-shadow: 0 12px 24px rgba(246, 196, 74, 0.35) !important;
  cursor: pointer !important;
}

#instructions button:hover{
  filter: brightness(0.98);
  transform: translateY(-1px);
}

#instructions button:active{
  transform: translateY(0px);
  box-shadow: 0 10px 18px rgba(246, 196, 74, 0.28) !important;
}

/* optional: make the table feel “card-like” but ONLY inside #instructions */
#instructions table{
  border-radius: 14px;
  overflow: hidden;
  background: #fff;
  box-shadow: 0 12px 24px rgba(15, 23, 42, 0.06);
}

/* responsive */
@media (max-width: 950px){
  #instructions{
    margin: 24px 16px;
    padding: 40px 22px 44px;
    min-height: auto;
  }
}
/* ✅ Only affects images inside questions (won’t touch your start screen) */
.question-media{
  margin: 14px 0 18px;
}

.question-media img{
  display: block;
  max-width: 320px;         /* ✅ smaller image */
  width: 100%;              /* responsive inside the max */
  height: auto;
  border: 1.5px solid #e5e7eb;   /* ✅ boxed */
  border-radius: 12px;
  padding: 10px;            /* ✅ space inside the box */
  background: #ffffff;

}

/* Optional: center the boxed image */
.question-media{
  display: flex;
  justify-content: center;
}
/* ✅ Bigger text for the QUESTION AREA only (safe: doesn’t touch header/footer/start screen) */
.container{
  font-size: 19px;           /* base size for question text */
  line-height: 1.6;
}

/* Question number bigger */
.container .question-number{
  font-size: 22px;
  font-weight: 800;
  margin-bottom: 10px;
}

/* Paragraphs spacing + size */
.container p{
  font-size: 19px;
  line-height: 1.65;
  margin: 12px 0;
}

/* Options bigger */
.container .options{
  margin-top: 18px;
  margin-bottom: 40px;
}

.container .options .option{
  font-size: 19px;
  line-height: 1.8;
}

/* Radio slightly larger */
.container input[type="radio"]{
  transform: scale(1.35);
  margin-right: 12px;
}

  </style>

<!-- TS_PATCH_FOOTER_FIXED_V1 -->
<style>
/* TS_PATCH_FOOTER_FIXED_V1: keep footer pinned and prevent overlap */
.footer{
  position: fixed !important;
  left: 0 !important;
  right: 0 !important;
  bottom: 0 !important;
  z-index: 999 !important;
}
#exam-interface .container{
  padding-bottom: 110px !important;
}
</style>
<!-- /TS_PATCH_FOOTER_FIXED_V1 -->
</head>
<body>

<div id="instructions">
    <h2 id="test-title">TMUA MOCK TEST 4</h2>


  <p><strong>Time allowed:</strong> 1 hour and 15 minutes</p>
  <p><strong>Calculators are not permitted.</strong></p>


<div style="display: flex; align-items: flex-end; gap: 20px; flex-wrap: wrap; margin: 20px 0;">
  <div>
    <label for="student-name" style="font-size: 16px;">Enter your full name:</label><br>
    <input type="text" id="student-name" placeholder="Full Name" style="padding: 8px; font-size: 16px; width: 240px; margin-top: 5px;">
  </div>

  <div>
    <label for="studentEmail" style="font-size: 16px;">Email (to receive results):</label><br>
    <input type="email" id="studentEmail" placeholder="example@email.com" style="padding: 8px; font-size: 16px; width: 240px; margin-top: 5px;">
  </div>

  <div>
    <label for="test-date" style="font-size: 16px;">Select your TMUA test session:</label><br>
    <select id="test-date" style="padding: 8px; font-size: 16px; width: 240px; margin-top: 5px;">
      <option value="October 2025">October 2026</option>
      <option value="January 2026">January 2027</option>
      <option value="October 2026">October 2027</option>
    </select>
  </div>

  <div style="margin-top: 28px;">
    <button onclick="startExam()" style="font-size: 16px; padding: 10px 20px;">🎯 Start Exam</button>
  </div>
</div>


<table style="border-collapse: collapse; width: 100%; font-size: 16px; margin-top: 20px;">
  <thead>
    <tr style="background: #f2f2f2;">
      <th style="border: 1px solid #ccc; padding: 10px;">Section</th>
      <th style="border: 1px solid #ccc; padding: 10px;">Topics Covered</th>
      <th style="border: 1px solid #ccc; padding: 10px;">No. of Questions</th>
      <th style="border: 1px solid #ccc; padding: 10px;">Time</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="border: 1px solid #ccc; padding: 10px; text-align: center;">Paper 2</td>
      <td style="border: 1px solid #ccc; padding: 10px;">
        <ul style="margin: 0; padding-left: 18px;">
          <li>Logic</li>
          <li>Proofs</li>
        </ul>
      </td>
      <td style="border: 1px solid #ccc; padding: 10px; text-align: center;">20</td>
      <td style="border: 1px solid #ccc; padding: 10px; text-align: center;">75 minutes</td>
    </tr>
  </tbody>
</table>

<p style="font-size: 16px; margin-top: 20px;">
 
  Choose the one correct answer for each question. There are <strong>no penalties for incorrect answers</strong> — marks are awarded only for correct responses.
  You should attempt <strong>all 20 questions</strong>.
</p>

<p style="font-size: 16px;">
  A countdown timer will begin once the test starts. You may navigate between questions at any time. The test will automatically submit when the time expires.
</p>


  <p style="font-weight: bold; font-size: 16px;">When you're ready, click the START EXAM button at the top to begin.</p>

</div>


<div id="exam-interface">
  <div class="header">
    <div><strong>(TMUA) Test of Mathematics for University Admission</strong></div>
    
    <div class="top-right">
      <div class="timer">
        ⏲️ Time Remaining <span id="time">75:00</span><br>
        📄 <span id="counter">1 of 20 (Paper 1)</span>
      </div>
      <button id="flag-btn" onclick="toggleFlag()">   ⚑ Flag for Review   </button>
    </div>
  </div>

  <div class="container" id="question-container">
    <!-- Questions will appear here -->
  </div>

  <div class="footer">
    <button id="prev-btn" onclick="prevQuestion()">&larr; Previous</button>
    <button id="next-btn" onclick="nextQuestion()">Next &rarr;</button>
    <button onclick="openNavigator()">Navigator</button>
    <button onclick="confirmSubmit()">Submit</button>
  </div>
</div>


<script>
const solutionPDF = "https://www.thrivingscholars.com/_files/ugd/98f2c5_d0af47aa08c34781b4d7b24059761d88.pdf";

let flags = new Array(20).fill(false); // Assuming 20 questions per section
let timeSpent = new Array(20).fill(0); // 20 questions
let lastTimestamp = Date.now(); // timestamp when current question was shown

let studentName = "";
let testSession = "";

function nextQuestion() {
  if (current < questions.length - 1) {
    timeSpent[current] += (Date.now() - lastTimestamp) / 1000; // record time spent
    current++;
    lastTimestamp = Date.now(); // reset timestamp for new question
    renderQuestion();
  }
}


function openNavigator() {
  document.getElementById("navigator-popup").style.display = "block";
}

function closeNavigator() {
  document.getElementById("navigator-popup").style.display = "none";
}

function jumpTo(index) {
timeSpent[current] += (Date.now() - lastTimestamp) / 1000;
current = index;
lastTimestamp = Date.now();
renderQuestion();
closeNavigator();
updateFlagButton();


}


function renderQuestion() {
  container.innerHTML = questions[current];
  counter.textContent = `${current + 1} of ${questions.length}`;
  MathJax.typesetPromise();
updateFlagButton();

  const inputs = document.querySelectorAll(`input[name='q${current + 1}']`);
  inputs.forEach(input => {
    if (userAnswers[current] === input.value) {
      input.checked = true;
    }
    input.addEventListener('change', () => {
      userAnswers[current] = input.value;
    });
  });

  // Disable Next button on last question
  const nextBtn = document.getElementById("next-btn");
  if (nextBtn) nextBtn.disabled = (current === questions.length - 1);

  // Disable Previous button on first question
  const prevBtn = document.getElementById("prev-btn");
  if (prevBtn) prevBtn.disabled = (current === 0);
}

function toggleFlag() {
  flags[current] = !flags[current];
  updateFlagButton();
  updateNavigatorButton(current);
}

function updateFlagButton() {
  const flagBtn = document.getElementById("flag-btn");
  if (!flagBtn) return;
  if (flags[current]) {
    flagBtn.classList.add("flagged");
  } else {
    flagBtn.classList.remove("flagged");
  }
}

  function confirmSubmit() {
  const confirmBox = confirm("Are you sure you want to submit the test?");
  if (confirmBox) {
    submitExam();
  }
}
  function confirmSubmit() {
  const confirmBox = confirm("Are you sure you want to submit the test?");
  if (confirmBox) {
    submitExam();
  }
}
  const correctAnswers = [
    "A", "C", "C", "B", "B",
    "E", "D", "A", "C", "D",
    "A", "D", "C", "H", "C",
    "D", "F", "B", "D", "B"
  ];

  let userAnswers = new Array(20).fill(null);
  let current = 0;
  let minutes = 75;
  let seconds = 0;
  let timerInterval;

  const timerDisplay = document.getElementById("time");
  const container = document.getElementById("question-container");
  const counter = document.getElementById("counter");

  const questions = [

    `<div class="question-number">Question 1</div>
<p>The polynomial \\( x^3 + (a - 3)x^2 + (b - 3a)x - 3b \\) has exactly two roots.</p>
<p>Which of the following is true?</p>

<div class="options">
  <label class="option"><input type="radio" name="q1" value="A"> \\( a^2 - 4b = 0 \\) is necessary</label><br>
  <label class="option"><input type="radio" name="q1" value="B"> \\( a^2 - 4b = 0 \\) is sufficient</label><br>
  <label class="option"><input type="radio" name="q1" value="C"> \\( a^2 - 4b = 0 \\) is necessary and sufficient</label><br>
  <label class="option"><input type="radio" name="q1" value="D"> \\( b^2 - 4a = 0 \\) is necessary</label><br>
  <label class="option"><input type="radio" name="q1" value="E"> None of the above</label>
</div>

`,

    `<div class="question-number">Question 2</div>
<p>Which of the following statements are true?</p>

<p>
I. being a square is a sufficient condition for being a rectangle.<br>
II. being less than 12 is a sufficient condition for being less than 20.<br>
III. being a rectangle is a necessary condition for being a square.<br>
IV. having 4 equal length sides is necessary and sufficient for being a square.<br>
V. an integer being less than 19.5 is sufficient, but not necessary for being less than 20.
</p>

<div class="options">
  <label class="option"><input type="radio" name="q2" value="A"> II and IV</label><br>
  <label class="option"><input type="radio" name="q2" value="B"> I, II and V</label><br>
  <label class="option"><input type="radio" name="q2" value="C"> I, II and III</label><br>
  <label class="option"><input type="radio" name="q2" value="D"> I only</label><br>
  <label class="option"><input type="radio" name="q2" value="E"> II only</label>
</div>

`,

    `  <div class="question-number">Question 3</div>

  <p>Let <i>I, II, III, IV</i> be some statements. Suppose that:</p>

  <p>
    \\[
    I \\rightarrow II \\rightarrow III, \\quad IV \\rightarrow \\text{not } III, \\quad \\text{not } I \\rightarrow II
    \\]
  </p>

  <p>
    where \\( a \\rightarrow b \\) means “if \\( a \\) is true, then \\( b \\) is true,” and <i>not</i> \\( a \\) is just the opposite of \\( a \\) — so if \\( a \\) is true, then <i>not</i> \\( a \\) is false and vice versa.
  </p>

  <p>Suppose \\( II \\) is a true statement. What can we say about the rest of the statements?</p>

  <div class="options">
    <label class="option"><input type="radio" name="q3" value="A"> <b>I:</b> true,&nbsp; <b>III:</b> true,&nbsp; <b>IV:</b> true</label><br>
    <label class="option"><input type="radio" name="q3" value="B"> <b>I:</b> could be either,&nbsp; <b>III:</b> true,&nbsp; <b>IV:</b> could be either</label><br>
    <label class="option"><input type="radio" name="q3" value="C"> <b>I:</b> could be either,&nbsp; <b>III:</b> true,&nbsp; <b>IV:</b> false</label><br>
    <label class="option"><input type="radio" name="q3" value="D"> <b>I:</b> false,&nbsp; <b>III:</b> true,&nbsp; <b>IV:</b> false</label><br>
    <label class="option"><input type="radio" name="q3" value="E"> <b>I:</b> true,&nbsp; <b>III:</b> false,&nbsp; <b>IV:</b> false</label>
  </div>


`,

    `<div class="question-number">Question 4</div>

<p>
Let <i>I</i> and <i>II</i> be two statements. You are asked to show that <i>I</i> if and only if <i>II</i>.
Which of the following does <b>not</b> prove the statement?
</p>

<div class="options">
  <label class="option"><input type="radio" name="q4" value="A"> <i>II</i> if <i>I</i>, and <i>I</i> if <i>II</i></label><br>
  <label class="option"><input type="radio" name="q4" value="B"> <i>not I</i> if <i>II</i>, and <i>not II</i> if <i>I</i></label><br>
  <label class="option"><input type="radio" name="q4" value="C"> <i>not II</i> if <i>not I</i>, and <i>not I</i> if <i>not II</i></label><br>
  <label class="option"><input type="radio" name="q4" value="D"> <i>not I</i> if <i>not II</i>, and <i>I</i> if <i>II</i></label>
</div>


`,

    `<div class="question-number">Question 5</div>

  <p>
    Which of the following are necessary and sufficient conditions for the equations 
    \\[
    y = x - 4 \\quad \\text{and} \\quad x^2 - 2y^2 = a
    \\]
    to have solutions?
  </p>

  <div class="options">
    <label class="option"><input type="radio" name="q5" value="A"> \\( a &lt; 32 \\)</label><br>
    <label class="option"><input type="radio" name="q5" value="B"> \\( a \\leq 32 \\)</label><br>
    <label class="option"><input type="radio" name="q5" value="C"> \\( a &gt; 32 \\)</label><br>
    <label class="option"><input type="radio" name="q5" value="D"> \\( a \\geq 32 \\)</label><br>
    <label class="option"><input type="radio" name="q5" value="E"> \\( a &lt; 16 \\)</label><br>
    <label class="option"><input type="radio" name="q5" value="F"> \\( a \\leq 16 \\)</label><br>
    <label class="option"><input type="radio" name="q5" value="G"> \\( a &gt; 16 \\)</label><br>
    <label class="option"><input type="radio" name="q5" value="H"> \\( a \\geq 16 \\)</label>
  </div>


`,

    `<div class="question-number">Question 6</div>

  <p>
    Consider the statement: “If \\( n \\) is an integer and \\( n^2 \\) is divisible by 4, then \\( n \\) is divisible by 4.”<br>
    How many counterexamples are there to this in the range \\( 50 \\leq n \\leq 100 \\)?
  </p>

  <div class="options">
    <label class="option"><input type="radio" name="q6" value="A"> 24</label><br>
    <label class="option"><input type="radio" name="q6" value="B"> 25</label><br>
    <label class="option"><input type="radio" name="q6" value="C"> 11</label><br>
    <label class="option"><input type="radio" name="q6" value="D"> 12</label><br>
    <label class="option"><input type="radio" name="q6" value="E"> 13</label>
  </div>

`,

    `<div class="question-number">Question 7</div>

  <p>
    Find a necessary and sufficient condition on \\( a \\), such that
  </p>

  <p style="text-align: center;">
    \\[
      \\sqrt{a - \\sqrt{a - \\sqrt{a - \\cdots}}}
      =
      \\cfrac{1}{a - \\cfrac{1}{a - \\cfrac{1}{a - \\cdots}}}
    \\]
  </p>

  <div class="options">
    <label class="option"><input type="radio" name="q7" value="A"> True for all real \\( a \\)</label><br>
    <label class="option"><input type="radio" name="q7" value="B"> \\( a = 0 \\)</label><br>
    <label class="option"><input type="radio" name="q7" value="C"> \\( a = 1 \\)</label><br>
    <label class="option"><input type="radio" name="q7" value="D"> \\( a = 2 \\)</label><br>
    <label class="option"><input type="radio" name="q7" value="E"> \\( a = \\sqrt{2} \\)</label>
  </div>`,

    `<div class="question-number">Question 8</div>

  <p>
    Cisco is trying to prove the circle theorem that angles from the same arc in the same segment are equal.
  </p>
  <p>
    Which of the following circle theorem rules should he use?
  </p>

  <div class="options">
    <label class="option"><input type="radio" name="q8" value="A"> "The angle at the centre of a circle is twice the angle at the circumference of the circle from the same arc"</label><br>
    <label class="option"><input type="radio" name="q8" value="B"> "The angle formed in a semicircle is always a right angle"</label><br>
    <label class="option"><input type="radio" name="q8" value="C"> "Opposite angles of a cyclic quadrilateral add up to 180 degrees"</label><br>
    <label class="option"><input type="radio" name="q8" value="D"> "Two tangents drawn from a point to a circle are equal"</label><br>
    <label class="option"><input type="radio" name="q8" value="E"> "The angle between a tangent and a chord is equal to the angle at the circumference in the alternate segment"</label><br>
    <label class="option"><input type="radio" name="q8" value="F"> "The perpendicular line from the centre of a circle to a chord bisects the chord"</label>
  </div>`,

    `<div class="question-number">Question 9</div>

<p>
  A maths teacher claims that the following proof contains a few errors.
  The proof is trying to solve the equation \\( 2 \\log_{5} x = 3 \\) for \\( x \\).
</p>


<p>
  \\( a \\rightarrow b \\) means &quot;if \\( a \\) is true, then \\( b \\) must also be true&quot; (a one-way implication).<br>
  \\( a \\leftrightarrow b \\) means &quot;\\( a \\) is true if and only if \\( b \\) is true&quot; (a two-way equivalence: each line can be derived from the other).
</p>

<p>
  So, for example:
</p>

<p>
 
  &bull; Writing &quot;Line 2 \\( \\rightarrow \\) Line 3&quot; means &quot;if Line 2 is correct, then Line 3 must also be correct&quot; (but Line 3 being true does <i>not</i> necessarily guarantee Line 2 is true).<br>
  &bull; Writing &quot;Line 3 \\( \\leftrightarrow \\) Line 4&quot; would claim that Line 3 and Line 4 are again true exactly in the same cases.
</p>

<p>
  Here is the student&apos;s proof, line by line:
</p>

<p>
  \\( 2 \\log_{5} x = 3 \\)&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp <br>
\\( \\leftrightarrow \\log_{5}(x^2) = 3 \\) &nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp (Line 2)<br>
\\( \\leftrightarrow x^2 = 5^3 = 125 \\) &nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp (Line 3)<br>
\\( \\rightarrow x = \\sqrt{125} = 5\\sqrt{5} \\)&nbsp&nbsp&nbsp&nbsp (Line 4)
</p>

<p>
  On which lines does the student&apos;s working contain an error (algebraic or logical)?
</p>

<div class="options">
  <label class="option"><input type="radio" name="q9" value="A"> lines 2, 3</label><br>
  <label class="option"><input type="radio" name="q9" value="B"> lines 3, 4</label><br>
  <label class="option"><input type="radio" name="q9" value="C"> lines 2, 4</label><br>
  <label class="option"><input type="radio" name="q9" value="D"> lines 2, 3, 4</label><br>
</div>
`,

    `<div class="question-number">Question 10</div>

  <p>
    Let \\( f \\) be a function such that \\( f(x) \\leq 0 \\) for all \\( x \\geq 0 \\). 
    Which one of the following is necessary for 
    \\( \\int_{-1}^{1} f(x)\\,dx > 0 \\)?
  </p>

  <div class="options">
    <label class="option"><input type="radio" name="q10" value="A"> \\( f(x) = -f(-x) \\) for all \\( x \\)</label><br>
    <label class="option"><input type="radio" name="q10" value="B"> \\( f(0) = 0 \\)</label><br>
    <label class="option"><input type="radio" name="q10" value="C"> \\( f(x) \\geq 0 \\) for all \\( x < 0 \\)</label><br>
    <label class="option"><input type="radio" name="q10" value="D"> \\( \\int_{-a}^{0} f(x)\\,dx > 0 \\) for some \\( a \\)</label><br>
    <label class="option"><input type="radio" name="q10" value="E"> \\( f(x) = f(-x) \\) for all \\( x \\)</label><br>
    <label class="option"><input type="radio" name="q10" value="F"> \\( f(-1) > 0 \\)</label>
  </div>`,


 `<div class="question-number">Question 11</div>

  <p>
    Consider the statement:<br><br>
    A number \\( p \\) is prime <u>only if</u> it can be expressed as 3 more than a multiple of 4,<br>
    i.e. \\( p = 4k + 3 \\) for some \\( k \\).<br><br>
    Which of the following are counterexamples to this?
  </p>

  <ul>
    <li>I. \\( p = 19 \\)</li>
    <li>II. \\( p = 27 \\)</li>
    <li>III. \\( p = 11 \\)</li>
  </ul>

  <div class="options">
    <label class="option"><input type="radio" name="q11" value="A"> None</label><br>
    <label class="option"><input type="radio" name="q11" value="B"> I only</label><br>
    <label class="option"><input type="radio" name="q11" value="C"> II only</label><br>
    <label class="option"><input type="radio" name="q11" value="D"> III only</label><br>
    <label class="option"><input type="radio" name="q11" value="E"> I & II only</label><br>
    <label class="option"><input type="radio" name="q11" value="F"> I & III only</label><br>
    <label class="option"><input type="radio" name="q11" value="G"> II & III only</label><br>
    <label class="option"><input type="radio" name="q11" value="H"> All three</label>
  </div>

`,


    `<div class="question-number">Question 12</div>

  <p>
    Consider the statement:<br><br>
    For all prime numbers \\( p \\), if \\( p = 4k + 1 \\) for some positive integer \\( k \\), it is true that there are positive integers \\( a \\) and \\( b \\) such that \\( p = a^2 + b^2 \\).<br><br>
    Which of the following, if they were true, would prove this statement false?
  </p>

  <div class="options">
    <label class="option"><input type="radio" name="q12" value="A"> For any \\( p \\) prime, if \\( p = a^2 + b^2 \\) for some \\( a, b \\), then \\( p = 4k + 1 \\).</label><br>
    <label class="option"><input type="radio" name="q12" value="B"> For any \\( p \\) prime, if \\( p \\ne a^2 + b^2 \\) for all \\( a, b \\), then \\( p = 4k + 1 \\).</label><br>
    <label class="option"><input type="radio" name="q12" value="C"> For any \\( p \\) prime, \\( p = 4k + 1 \\) if and only if \\( p = a^2 + b^2 \\) for some \\( a, b \\).</label><br>
    <label class="option"><input type="radio" name="q12" value="D"> There exists a \\( p \\) prime such that \\( p = 4k + 1 \\) and \\( p \\ne a^2 + b^2 \\) for all \\( a, b \\).</label><br>
    <label class="option"><input type="radio" name="q12" value="E"> There exists a \\( p \\) prime such that \\( p = a^2 + b^2 \\) for some \\( a, b \\) or \\( p = 4k + 1 \\).</label>
  </div>`,

    `<div class="question-number">Question 13</div>

  <p>
    Consider the following proof of the alternate segment theorem:
  </p>

  <ol type="I">
    <li>Let a tangent touch a circle at <i>A</i> and let <i>B, C</i> be two points on the circumference of the circle. Let the angle between the line <i>CA</i> and the tangent be \\( \\alpha^\\circ \\).</li>
    <li>By drawing radii from <i>C</i> and <i>A</i> to the centre of the circle, which we will call <i>O</i>, we know that the angle between <i>OA</i> and the tangent is \\( 90^\\circ \\).</li>
    <li>This means the angle \\( \\angle BAO \\) is \\( (90 - \\alpha)^\\circ \\).</li>
    <li>As the triangle \\( OBA \\) is isosceles, the angle \\( \\angle OBA \\) is also \\( (90 - \\alpha)^\\circ \\).</li>
    <li>Then the angle \\( \\angle AOB \\) is \\( 180^\\circ - (180 - 2\\alpha)^\\circ = 2\\alpha^\\circ \\).</li>
    <li>This means the angle \\( \\angle ACB \\) is \\( \\left(\\frac{2\\alpha}{2}\\right)^\\circ = \\alpha^\\circ \\).</li>
  </ol>

  <p>Where is the first error?</p>

  <div class="options">
    <label class="option"><input type="radio" name="q13" value="A"> The proof is correct.</label><br>
    <label class="option"><input type="radio" name="q13" value="B"> Line (II)</label><br>
    <label class="option"><input type="radio" name="q13" value="C"> Line (III)</label><br>
    <label class="option"><input type="radio" name="q13" value="D"> Line (IV)</label><br>
    <label class="option"><input type="radio" name="q13" value="E"> Line (V)</label><br>
    <label class="option"><input type="radio" name="q13" value="F"> Line (VI)</label>
  </div>`,

    `<div class="question-number">Question 14</div>

  <p>Consider the following diagram:</p>

  <div style="flex: 0 0 40%;">
      <img src="https://static.wixstatic.com/media/98f2c5_b5270dc1ecb74bd2b96f7f34bfa6bc6f~mv2.png" 
           alt="Rhombus Diagram" 
           style="width: 100%; max-width: 250px; height: auto;">
  </div>

  <p>Which of the following are necessary for \\(ABCD\\) to be a rhombus?</p>

  <ol type="I">
    <li>Angle \\( \\angle PAD = 45^\\circ \\)</li>
    <li>Length \\( CA = 2r \\) (\\( r \\) is the radius of the circle)</li>
    <li>Lengths \\( CD = CB \\)</li>
  </ol>

  <div class="options">
    <label class="option"><input type="radio" name="q14" value="A"> None</label><br>
    <label class="option"><input type="radio" name="q14" value="B"> I only</label><br>
    <label class="option"><input type="radio" name="q14" value="C"> II only</label><br>
    <label class="option"><input type="radio" name="q14" value="D"> III only</label><br>
    <label class="option"><input type="radio" name="q14" value="E"> I &amp; II only</label><br>
    <label class="option"><input type="radio" name="q14" value="F"> I &amp; III only</label><br>
    <label class="option"><input type="radio" name="q14" value="G"> II &amp; III only</label><br>
    <label class="option"><input type="radio" name="q14" value="H"> All three</label>
  </div>`,

    `<div class="question-number">Question 15</div>

<p>A student tried to solve the following problem:</p>

<p style="text-align: center;">
  \\[
    \\frac{2^{18x}}{2^{3x^2} \\cdot 4^6} > 1
  \\]
</p>

<p>Here is his solution:</p>

<ol type="I">
  <li>
    <p style="text-align: left;">
      \\[
        \\frac{2^{18x}}{2^{3x^2} \\cdot 4^6}
        = 2^{18x - 3x^2} \\cdot 4^{-6}
        > 1
      \\]
    </p>
  </li>
  <li>
    <p style="text-align: left;">
      \\[
        8^{6x - 3x^2} \\cdot 4^{-6} > 1
      \\]
    </p>
  </li>
  <li>
    <p style="text-align: left;">
      \\[
        8^{6x - x^2 - 3} > 1
      \\]
    </p>
  </li>
  <li>
    <p style="text-align: left;">
      \\[
        6x - x^2 - 3 > 0
      \\]
    </p>
  </li>
  <li>
    <p style="text-align: left;">
      Critical values are:
      \\[
        x = \\frac{6 \\pm \\sqrt{36 - 12}}{2} = 3 \\pm \\sqrt{6}
      \\]
    </p>
  </li>
  <li>
    <p style="text-align: left;">
      So the solution is:
      \\[
        3 - \\sqrt{6} < x < 3 + \\sqrt{6}
      \\]
    </p>
  </li>
</ol>

<p><b>Where is the first error if any?</b></p>

<div class="options">
  <label class="option"><input type="radio" name="q15" value="A"> The proof is correct</label><br>
  <label class="option"><input type="radio" name="q15" value="B"> Line (I)</label><br>
  <label class="option"><input type="radio" name="q15" value="C"> Line (II)</label><br>
  <label class="option"><input type="radio" name="q15" value="D"> Line (III)</label><br>
  <label class="option"><input type="radio" name="q15" value="E"> Line (IV)</label><br>
  <label class="option"><input type="radio" name="q15" value="F"> Line (V)</label><br>
  <label class="option"><input type="radio" name="q15" value="G"> Line (VI)</label>
</div>
`,

    `<div class="question-number">Question 16</div>

<p>Consider the following proof by contradiction that \\( \\sqrt{12} \\) is irrational.</p>

<ol type="I">
  <li>
    <p style="text-align: left;">
      Suppose \\( \\sqrt{12} \\) is rational. Then we can express it in the form 
      \\( \\sqrt{12} = \\frac{a}{b} \\) for some \\( a \\) and \\( b \\) with no common divisors.
    </p>
  </li>
  <li>
    <p style="text-align: left;">
      \\[
        12 = \\frac{a^2}{b^2}
      \\]
    </p>
  </li>
  <li>
    <p style="text-align: left;">
      \\[
        12b^2 = a^2
      \\]
    </p>
  </li>
  <li>
    <p style="text-align: left;">
      12 divides \\( a^2 \\), which means that 12 must also divide \\( a \\), i.e. \\( a = 12c \\) for some integer \\( c \\).
    </p>
  </li>
  <li>
    <p style="text-align: left;">
      This means \\( 144c^2 = 12b^2 \\), i.e. \\( b^2 = 12c^2 \\)
    </p>
  </li>
  <li>
    <p style="text-align: left;">
      Using the logic of line (IV), \\( b = 12d \\) for some integer \\( d \\).
    </p>
  </li>
  <li>
    <p style="text-align: left;">
      But then \\( a \\) and \\( d \\) both have 12 as a divisor, which is a contradiction.
    </p>
  </li>
</ol>

<p><b>Where is the first error if any?</b></p>

<div class="options">
  <label class="option"><input type="radio" name="q16" value="A"> The proof is correct</label><br>
  <label class="option"><input type="radio" name="q16" value="B"> Line (I)</label><br>
  <label class="option"><input type="radio" name="q16" value="C"> Line (II)</label><br>
  <label class="option"><input type="radio" name="q16" value="D"> Line (IV)</label><br>
  <label class="option"><input type="radio" name="q16" value="E"> Line (V)</label><br>
  <label class="option"><input type="radio" name="q16" value="F"> Line (VI)</label><br>
  <label class="option"><input type="radio" name="q16" value="G"> Line (VII)</label>
</div>
`,


    `<div class="question-number">Question 17</div>

<p>
  Consider the graphs of the two functions 
  \\( y = mx - 10 \\) and \\( y = \\log_2 x \\).<br>
Consider the following two conditions:
</p>

<p style="margin-left: 1em;">
 I. \\( m &lt; 0 \\)<br>
II. \\( m &gt; 10 \\)
</p>

<p>
  Which of the following correctly identifies the sufficiency and necessity of the two conditions above in relation to the statement: 'There are intersection points of the two graphs'?
</p>

<div class="options">
  <label class="option"><input type="radio" name="q17" value="A"> Both necessary and sufficient.</label><br>
  <label class="option"><input type="radio" name="q17" value="B"> Both necessary.</label><br>
  <label class="option"><input type="radio" name="q17" value="C"> Both sufficient.</label><br>
  <label class="option"><input type="radio" name="q17" value="D"> 1 is sufficient, the other is necessary.</label><br>
  <label class="option"><input type="radio" name="q17" value="E"> Only 1 is necessary.</label><br>
  <label class="option"><input type="radio" name="q17" value="F"> Only 1 is sufficient.</label><br>
  <label class="option"><input type="radio" name="q17" value="G"> Neither is necessary or sufficient.</label>
</div>
`,

    `<div class="question-number">Question 18</div>

<p>
  Consider the following proof by induction that \\( 3 \\times 7^n + 6 \\) is divisible by 9 for all non-negative \\( n \\):
</p>

<ol type="I">
  <li>
    Check the base case, \\( n = 1 \\); \\( 3 \\times 7 + 6 = 27 \\), which is indeed divisible by 9.
  </li>
  <li>
    First, we suppose \\( 3 \\times 7^n + 6 \\) is divisible by 9 for \\( n \\leq k \\), i.e.<br>
    \\( 3 \\times 7^k + 6 = 9M \\) for some integer \\( M \\).
  </li>
  <li>
    Then \\( 3 \\times 7^{k+1} + 6 = 7 \\times (3 \\times 7^k) + 6 \\)
  </li>
  <li>
    This means \\( 7 \\times (3 \\times 7^k) + 6 = 7(9M - 6) + 6 \\)
  </li>
  <li>
    Which means \\( 3 \\times 7^{k+1} + 6 = 9(7M - 4) \\)
  </li>
  <li>
    So \\( 3 \\times 7^{k+1} + 6 \\) is also divisible by 9, which means \\( 3 \\times 7^n + 6 \\) is divisible by 9 for all non-negative \\( n \\).
  </li>
</ol>

<p><b>Where is the first error if any?</b></p>

<div class="options">
  <label class="option"><input type="radio" name="q18" value="A"> The proof is correct.</label><br>
  <label class="option"><input type="radio" name="q18" value="B"> Line (I)</label><br>
  <label class="option"><input type="radio" name="q18" value="C"> Line (II)</label><br>
  <label class="option"><input type="radio" name="q18" value="D"> Line (III)</label><br>
  <label class="option"><input type="radio" name="q18" value="E"> Line (IV)</label><br>
  <label class="option"><input type="radio" name="q18" value="F"> Line (V)</label><br>
  <label class="option"><input type="radio" name="q18" value="G"> Line (VI)</label>
</div>
`,


    `<div class="question-number">Question 19</div>

<p>
  Which of the following functions is a counterexample to the statement below?
</p>

<p>"If a function has 3 distinct real roots, it is cubic."</p>

<div class="options">
  <label class="option"><input type="radio" name="q19" value="A">
  sin&nbsp; <span>\\( x \\)</span> in the range&nbsp;<span>\\( 0 &lt; x &lt; 2\\pi \\)</span>
  </label><br>

  <label class="option"><input type="radio" name="q19" value="B">
   <span>\\( (x - 1)(x - 2)(x - 3)(x - 4) \\)</span>
  </label><br>

  <label class="option"><input type="radio" name="q19" value="C">
    cos&nbsp;<span>\\( x \\)</span> in the range&nbsp;<span>\\( 0 &lt; x &lt; 2\\pi \\)</span>
  </label><br>

  <label class="option"><input type="radio" name="q19" value="D">
    <span>\\( (x - 1)^2(x - 2)(x - 3) \\)</span>
  </label><br>

  <label class="option"><input type="radio" name="q19" value="E">
    <span>\\( (x + 1)(x^2 - x + 5) \\)</span>
  </label><br>

  <label class="option"><input type="radio" name="q19" value="F">
  <span>\\( (x - 3)(x + 1)(x - 4) \\)</span>
  </label>
</div>



`,

    `<div class="question-number">Question 20</div>

<p>
  Consider this student’s attempt at finding the solutions to:
</p>

<p style="text-align: center;">
  \\[
    \\sqrt{x + 3} = 3x - 1
  \\]
</p>




<ol type="I">
  <li>\\( x + 3 = (3x - 1)^2 \\)</li>
  <li>\\( x + 3 = 9x^2 - 6x + 1 \\)</li>
  <li>\\( 0 = 9x^2 - 7x - 2 \\)</li>
  <li>\\( 0 = (x - 1)(9x + 2) \\)</li>
<li>So \\( x = 1 \\) <i>and</i> \\( -\\frac{2}{9} \\)</li>
</ol>

<p><b>Is this correct?</b></p>

<div class="options">
  <label class="option"><input type="radio" name="q20" value="A"> Both answers are correct.</label><br>
  <label class="option"><input type="radio" name="q20" value="B"> Only one is right and it’s due to an error in line (I).</label><br>
  <label class="option"><input type="radio" name="q20" value="C"> Only one is right and it’s due to an error in lines (II) and (III).</label><br>
  <label class="option"><input type="radio" name="q20" value="D"> Only one is right and it’s due to an error in line (IV).</label><br>
  <label class="option"><input type="radio" name="q20" value="E"> Neither is right and it’s due to an error in line (I).</label><br>
  <label class="option"><input type="radio" name="q20" value="F"> Neither is right and it’s due to an error in lines (II) and (III).</label><br>
  <label class="option"><input type="radio" name="q20" value="G"> Neither is right and it’s due to an error in line (IV).</label>
</div>

`,

  ];




function startExam() {
  const nameInput = document.getElementById("student-name").value.trim();
  const emailInput = document.getElementById("studentEmail").value.trim();
  const testSelect = document.getElementById("test-date").value;

  if (!nameInput) {
    alert("Please enter your full name before starting the exam.");
    return;
  }

  if (!emailInput || !emailInput.includes("@")) {
    alert("Please enter a valid email address.");
    return;
  }

  studentName = nameInput;
  testSession = testSelect;

  document.getElementById("instructions").style.display = "none";
  document.getElementById("exam-interface").style.display = "block";
  timerInterval = setInterval(updateTimer, 1000);
  renderQuestion();
}



  function updateTimer() {
    if (minutes === 0 && seconds === 0) {
      clearInterval(timerInterval);
      submitExam();
      return;
    }
    if (seconds === 0) {
      minutes--;
      seconds = 59;
    } else {
      seconds--;
    }
    const mm = minutes.toString().padStart(2, '0');
    const ss = seconds.toString().padStart(2, '0');
    timerDisplay.textContent = `${mm}:${ss}`;
  }

  
function prevQuestion() {
  if (current > 0) {
    timeSpent[current] += (Date.now() - lastTimestamp) / 1000; // record time spent
    current--;
    lastTimestamp = Date.now(); // reset timestamp for new question
    renderQuestion();
  }
}

function updateNavigatorButton(index) {
  const btn = document.getElementById(`nav-btn-${index}`);
  if (!btn) return;
  if (flags[index]) {
    btn.style.backgroundColor = "gold";
    btn.style.fontWeight = "bold";
    btn.textContent = `⚑ ${index + 1}`;
  } else {
    btn.style.backgroundColor = "";
    btn.style.fontWeight = "normal";
    btn.textContent = `${index + 1}`;
  }
}

function submitExam() {
  clearInterval(timerInterval);

  // ⏱️ Record final question time
  timeSpent[current] += (Date.now() - lastTimestamp) / 1000;

  // ✅ Calculate score
  let score = 0;
  for (let i = 0; i < correctAnswers.length; i++) {
    if (userAnswers[i] === correctAnswers[i]) {
      score++;
    }
  }

  // ✅ Prepare timestamp
  const timestamp = new Date().toLocaleString();

  // ✅ Find incorrect question numbers
  let incorrectList = [];
  for (let i = 0; i < correctAnswers.length; i++) {
    if (userAnswers[i] !== correctAnswers[i]) {
      incorrectList.push(i + 1);
    }
  }

let timeTableHTML = "";
for (let i = 0; i < 20; i++) {
  const seconds = Math.round(timeSpent[i]);
  const mm = String(Math.floor(seconds / 60)).padStart(2, '0');
  const ss = String(seconds % 60).padStart(2, '0');
  const yourAns = userAnswers[i] ?? "-";
  const correctAns = correctAnswers[i];
  const status = yourAns === correctAns ? "✅" : (yourAns === "-" ? "—" : "❌");

  timeTableHTML += `
    <tr>
      <td style="border: 1px solid #ccc; padding: 6px;">${i + 1}</td>
      <td style="border: 1px solid #ccc; padding: 6px;">${yourAns}</td>
      <td style="border: 1px solid #ccc; padding: 6px;">${correctAns}</td>
      <td style="border: 1px solid #ccc; padding: 6px;">${mm}:${ss}</td>
      <td style="border: 1px solid #ccc; padding: 6px;">${status}</td>
    </tr>\n`; // ✅ Keep line break
}

let plainTextSummary = "Paper 2 Analysis\n";

for (let i = 0; i < 20; i++) {
  const seconds = Math.round(timeSpent[i]);
  const mm = String(Math.floor(seconds / 60)).padStart(2, '0');
  const ss = String(seconds % 60).padStart(2, '0');
  const yourAns = userAnswers[i] ?? "-";
  const correctAns = correctAnswers[i];
  plainTextSummary += `Q.${i + 1}  Correct: ${correctAns}   Yours: ${yourAns}   Time: ${mm}:${ss}\n`;
}


const studentEmail = document.getElementById("studentEmail").value;
const testTitle = document.getElementById("test-title").textContent.trim();

emailjs.send("service_5l4w8x2", "template_j1jrs4o", {
  name: studentName,
  session: testSession,
  score: `${score} / ${correctAnswers.length}`,
  timestamp: new Date().toLocaleString(),
  incorrect: incorrectList.length > 0 ? incorrectList.join(", ") : "None",
  paper1: plainTextSummary.trim(),
  to_email: studentEmail,
  solution_link: solutionPDF,
  test_title: testTitle
})
.then(res => console.log("✅ Email sent", res))
.catch(err => console.error("❌ Email error", err));



  // ✅ Select emoji
  let emoji = score >= 14 ? "😊"  : "😐";

  // ✅ Generate time table
  let timeRows = "";
  for (let i = 0; i < timeSpent.length; i++) {
    const seconds = Math.round(timeSpent[i]);
    const mm = String(Math.floor(seconds / 60)).padStart(2, '0');
    const ss = String(seconds % 60).padStart(2, '0');
    const status = userAnswers[i] ? (userAnswers[i] === correctAnswers[i] ? "✅" : "❌") : "—";
    timeRows += `
      <tr>
        <td style="border: 1px solid #ccc; padding: 8px;">${i + 1}</td>
        <td style="border: 1px solid #ccc; padding: 8px;">${userAnswers[i] || "-"}</td>
        <td style="border: 1px solid #ccc; padding: 8px;">${correctAnswers[i]}</td>
        <td style="border: 1px solid #ccc; padding: 8px;">${mm}:${ss}</td>
        <td style="border: 1px solid #ccc; padding: 8px;">${status}</td>
      </tr>`;
  }

  // ✅ Display final screen
  container.innerHTML = `
    <div style="display: flex; flex-direction: column; align-items: center; padding: 40px;">
      <div style="font-size: 160px;">${emoji}</div>
      <h1>Exam Completed</h1>
<h1>The detailed Score Report has been emailed to you ✅ </h1>

      <table style="border-collapse: collapse; margin-top: 30px; font-size: 18px;">
        <tr style="background: #f9f9f9;">
          <td style="border: 1px solid #ccc; padding: 10px 20px;"><strong>Name</strong></td>
          <td style="border: 1px solid #ccc; padding: 10px 20px;">${studentName}</td>
        </tr>
        <tr style="background: #f9f9f9;">
          <td style="border: 1px solid #ccc; padding: 10px 20px;"><strong>Your Score</strong></td>
          <td style="border: 1px solid #ccc; padding: 10px 20px;"><strong>${score} / 20</strong></td>
        </tr>
        <tr>
          <td style="border: 1px solid #ccc; padding: 10px 20px;">Threshold Score</td>
          <td style="border: 1px solid #ccc; padding: 10px 20px;">16+ / 20</td>
        </tr>
        <tr>
          <td style="border: 1px solid #ccc; padding: 10px 20px;">Average Test Taker Score</td>
          <td style="border: 1px solid #ccc; padding: 10px 20px;">9 / 20</td>
        </tr>
      </table>

      <h3 style="margin-top: 40px;">Paper 2 Analysis</h3>
      <table style="border-collapse: collapse; margin-top: 10px; font-size: 16px; width: 100%; max-width: 600px;">
        <thead style="background: #eee;">
          <tr>
            <th style="border: 1px solid #ccc; padding: 8px;">Q#</th>
            <th style="border: 1px solid #ccc; padding: 8px;">Your Ans</th>
            <th style="border: 1px solid #ccc; padding: 8px;">Correct</th>
            <th style="border: 1px solid #ccc; padding: 8px;">Time</th>
            <th style="border: 1px solid #ccc; padding: 8px;">Status</th>
          </tr>
        </thead>
        <tbody>${timeRows}</tbody>
      </table>

      <h3 style="margin-top: 40px;">📄 View Full Solutions</h3>
  <iframe 
  id="solution-frame"
  width="100%" 
  height="600px" 
  style="border: 1px solid #ccc; margin-top: 10px;">
</iframe>
    </div>
  `;
document.getElementById("solution-frame").src = solutionPDF;

  document.querySelector(".footer").style.display = "none";
}

function generateNavigatorButtons() {
  const grid = document.getElementById("navigator-grid");
  for (let i = 0; i < 20; i++) {
    const btn = document.createElement("button");
    btn.id = `nav-btn-${i}`;
    btn.textContent = i + 1;
    btn.style.padding = "10px";
    btn.onclick = function () {
      jumpTo(i);
    };
    grid.appendChild(btn);
  }
}

window.onload = function () {
  generateNavigatorButtons();
  for (let i = 0; i < flags.length; i++) {
    updateNavigatorButton(i);
  }
};


</script>
<div id="navigator-popup" style="display:none; position:fixed; top:20%; left:50%; transform:translateX(-50%);
 background:white; border:2px solid #0074c1; border-radius:10px; padding:20px; z-index:9999; width:300px; box-shadow:0 5px 15px rgba(0,0,0,0.3);">
  <h3 style="margin-top: 0; color:#0074c1;">Jump to Question</h3>
  <div id="navigator-grid" style="display:grid; grid-template-columns: repeat(5, 1fr); gap:10px; margin-bottom: 20px;">
    <!-- Buttons will be generated here -->
  </div>
  <button onclick="closeNavigator()" style="background:#ccc; padding:6px 12px;">Close</button>
</div>
<script src="https://cdn.jsdelivr.net/npm/emailjs-com@3/dist/email.min.js"></script>
<script>
  (function () {
    emailjs.init("9jjn3yBHl6S88hITy"); // Replace with your actual public key
  })();
</script>

<!-- TS_PATCH_KEYS_RC_V1 -->
<script>
(function(){
  function disableRightClick(){
    document.addEventListener("contextmenu", function(e){
      e.preventDefault();
    }, true);
  }

  function isTypingTarget(t){
    if (!t) return false;
    var tag = (t.tagName || "").toUpperCase();
    return t.isContentEditable || tag === "INPUT" || tag === "TEXTAREA" || tag === "SELECT";
  }

  function bindHotkeys(){
    window.addEventListener("keydown", function(e){
      if (!e.altKey) return;
      if (e.ctrlKey || e.metaKey) return;
      if (isTypingTarget(e.target)) return;

      var k = (e.key || "").toLowerCase();
      if (k === "n") {
        e.preventDefault(); e.stopPropagation();
        if (typeof nextQuestion === "function") nextQuestion();
        else if (window.NAV && typeof window.NAV.next === "function") window.NAV.next();
      } else if (k === "p") {
        e.preventDefault(); e.stopPropagation();
        if (typeof prevQuestion === "function") prevQuestion();
        else if (window.NAV && typeof window.NAV.prev === "function") window.NAV.prev();
      }
    }, true);
  }

  function bootPatch(){
    try { bindHotkeys(); } catch(_) {}
    try { disableRightClick(); } catch(_) {}
  }

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", bootPatch);
  } else {
    bootPatch();
  }
})();
</script>
<!-- /TS_PATCH_KEYS_RC_V1 -->
<!-- TS_PATCH_WORKSPACE_BTN_V2 -->
<style>
  .ts-workspace-btn{
    position: fixed;
    left: 14px;
    bottom: 7px;
    z-index: 2000;
    display: inline-flex;
    align-items: center;
    gap: 8px;

    padding: 9px 12px;
    border-radius: 999px;

    background: rgba(255,255,255,0.92);
    border: 1px solid rgba(17,24,39,0.18);
    box-shadow: 0 10px 24px rgba(0,0,0,0.10);

    color: rgba(17,24,39,0.86);
    font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.02em;

    cursor: pointer;
    user-select: none;
  }
  .ts-workspace-btn:hover{ background: rgba(255,255,255,0.98); }
  .ts-workspace-btn:active{ transform: translateY(1px); }
  .ts-workspace-btn:focus{ outline: none; }
</style>

<button type="button" class="ts-workspace-btn" id="tsWorkspaceBtn" aria-label="Back to TMUA Workspace">
  &larr; TMUA Workspace
</button>

<script>
(function(){
  var DASH_URL = "https://apps.thrivingscholars.com/dashboard";

  // When true, we warn on refresh/close/navigate-away.
  // We disable it only when the user confirms leaving via our button.
  var tsWarnOnLeave = true;

  function confirmLeave(){
    return window.confirm("Are you sure you want to go back? Your progress on the current test will be lost.");
  }

  function goWorkspace(){
    window.location.href = DASH_URL;
  }

  function bindBtn(){
    var b = document.getElementById("tsWorkspaceBtn");
    if (!b) return;
    if (b.getAttribute("data-bound")) return;
    b.setAttribute("data-bound","1");

    b.addEventListener("click", function(e){
      e.preventDefault();
      e.stopPropagation();

      if (confirmLeave()) {
        tsWarnOnLeave = false; // allow navigation without extra browser prompt
        goWorkspace();
      }
    }, true);
  }

  // Refresh/close/tab change warnings:
  window.addEventListener("beforeunload", function(e){
    if (!tsWarnOnLeave) return;
    // Modern browsers ignore custom text, but setting returnValue triggers the prompt.
    e.preventDefault();
    e.returnValue = "";
    return "";
  });

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", bindBtn);
  } else {
    bindBtn();
  }
})();
</script>
<!-- /TS_PATCH_WORKSPACE_BTN_V2 -->
</body>
</html>

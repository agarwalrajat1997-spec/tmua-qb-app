import Link from "next/link";

export const dynamic = "force-dynamic";

export default function ESATPracticeTestsPage() {
  return (
    <main
      style={{
        minHeight: "100vh",
        padding: 32,
        background: "#fffaf2",
        fontFamily: "Inter, system-ui, Arial, sans-serif",
      }}
    >
      <section
        style={{
          maxWidth: 920,
          margin: "0 auto",
          background: "#fff",
          border: "1px solid #e5e7eb",
          borderRadius: 18,
          padding: 26,
          boxShadow: "0 10px 28px rgba(17,24,39,.08)",
        }}
      >
        <div style={{ fontSize: 13, fontWeight: 900, letterSpacing: ".12em", textTransform: "uppercase", color: "#7A1F24" }}>
          Thriving Scholars · ESAT
        </div>

        <h1 style={{ margin: "10px 0 10px", fontSize: 36, lineHeight: 1.1 }}>
          ESAT Practice Tests
        </h1>

        <p style={{ color: "#4b5563", fontSize: 16, lineHeight: 1.7 }}>
          This section is now wired into the ESAT dashboard layout. Add ESAT mock/test files here when the practice-test pack is ready.
        </p>

        <Link
          href="/esat"
          style={{
            display: "inline-flex",
            marginTop: 18,
            background: "#7A1F24",
            color: "#fff",
            padding: "12px 16px",
            borderRadius: 12,
            textDecoration: "none",
            fontWeight: 900,
          }}
        >
          Back to ESAT Dashboard
        </Link>
      </section>
    </main>
  );
}
